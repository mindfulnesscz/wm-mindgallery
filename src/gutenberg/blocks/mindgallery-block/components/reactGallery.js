import * as React from 'react';
const Gallery = ({ className, attributes }) => {
    const { captions, customFontSize, fontSize, fullwidth, gutter, gutterMobile, images, linkTo, } = attributes;
    return (React.createElement("div", { className: className },
        React.createElement("h1", null, "Ahoj vole kua")));
};
//# sourceMappingURL=reactGallery.js.map